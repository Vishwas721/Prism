# Prism Client

Bootstrap the Vite + React frontend here. Suggested next steps:

1. `npm create vite@latest client -- --template react` (or `pnpm`/`yarn`)
2. Install deps and run `npm run dev`
3. Point API requests to `http://localhost:8000`
